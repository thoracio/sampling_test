val to_file : text:string -> file:string -> unit
(*[to_file text:t file:f] writes the string t to the file f, if the file does
 *not exists, it creates, to be used to save the sampling result to a file
 *)
